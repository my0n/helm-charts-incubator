apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskd-admin.fullname" . }}
  labels:
    {{- include "taskd-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "taskd-admin.selectorLabels" . | nindent 4 }}

suite: service uses service.port
templates:
  - service.yaml
tests:
  - it: uses service.port
    set:
      service.port: 9000
    asserts:
      - contains:
          path: spec.ports
          content:
            port: 9000
            targetPort: 80
            protocol: TCP
            name: http
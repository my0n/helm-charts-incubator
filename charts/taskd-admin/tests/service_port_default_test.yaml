suite: service uses service.port default
templates:
  - service.yaml
tests:
  - it: uses service.port default
    asserts:
      - contains:
          path: spec.ports
          content:
            port: 80
            targetPort: 80
            protocol: TCP
            name: http
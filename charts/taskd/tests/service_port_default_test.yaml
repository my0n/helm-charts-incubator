suite: service uses service.port default
templates:
  - service.yaml
tests:
  - it: uses service.port default
    asserts:
      - contains:
          path: spec.ports
          content:
            port: 53589
            targetPort: 53589
            protocol: TCP
            name: taskd